apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: memory-burner-ss
  labels:
    environment: production
    app: memory-burner-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: memory-burner-deployment
  template:
    metadata:
      labels:
        app: memory-burner-deployment
    spec:
      containers:
      - name: memory-burner-container
        image: usman476/resource-burner:latest
        env:
        - name: WORKLOAD_TYPE
          value: "memory-burner"
        command: ["python3"]
        args: ["burner.py"]
        imagePullPolicy: "Always"
        resources:
          requests:
            cpu: 1000m
            memory: 1024Mi
      restartPolicy: Always